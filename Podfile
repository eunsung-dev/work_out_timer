# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'work-out-timer' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for work-out-timer
    pod 'SRCountdownTimer'
        
  target 'work-out-timerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'work-out-timerUITests' do
    # Pods for testing
  end

end
